#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

export BREAKD_INSTANCE=dev

if (($# == 0)); then
	set -- daemon
fi

exec cargo run --locked -- "$@"
